import 'package:flutter/material.dart';

class FeesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Fees Payment")),
      body: Center(
        child: Text("Fees Feature Coming Soon"),
      ),
    );
  }
}
